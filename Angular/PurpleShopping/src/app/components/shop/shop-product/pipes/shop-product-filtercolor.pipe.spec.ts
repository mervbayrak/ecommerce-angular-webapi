import { ShopProductFiltercolorPipe } from './shop-product-filtercolor.pipe';

describe('ShopProductFiltercolorPipe', () => {
  it('create an instance', () => {
    const pipe = new ShopProductFiltercolorPipe();
    expect(pipe).toBeTruthy();
  });
});
