import { ShopProductSortPipe } from './shop-product-sort.pipe';

describe('ShopProductSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ShopProductSortPipe();
    expect(pipe).toBeTruthy();
  });
});
