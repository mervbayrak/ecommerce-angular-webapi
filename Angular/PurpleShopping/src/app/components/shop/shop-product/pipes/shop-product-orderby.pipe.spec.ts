import { ShopProductOrderbyPipe } from './shop-product-orderby.pipe';

describe('ShopProductOrderbyPipe', () => {
  it('create an instance', () => {
    const pipe = new ShopProductOrderbyPipe();
    expect(pipe).toBeTruthy();
  });
});
